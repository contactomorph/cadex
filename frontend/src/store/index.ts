import Vue from 'vue'
import Vuex from 'vuex'
import { Story, Player } from '../backend'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
